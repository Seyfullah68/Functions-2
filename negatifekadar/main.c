#include <stdio.h>
#include <stdlib.h>

void negatif (int a)
{
    while (a>=0)
    {
        scanf("%d",&a);
    }

}

int main()
{
    int x;
    if(x>=0)
    {
      negatif(x);
    }


    return 0;
}
